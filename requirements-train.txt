# Training dependencies (train.py). Not needed to run the server.
# On Kaggle, torch/torchvision are pre-installed — just: pip install timm onnx onnxscript onnxruntime
torch
torchvision
timm
scikit-learn
matplotlib
onnx
onnxruntime
numpy
pillow
