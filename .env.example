# Copy to .env and fill in real values (.env is gitignored).
# Postgres connection string — adjust user/host as needed.
DATABASE_URL=postgresql://USER@localhost/krishiai

# JWT signing key — use a long random string in production, e.g.:
#   python -c "import secrets; print(secrets.token_urlsafe(48))"
SECRET_KEY=replace-with-a-long-random-secret
